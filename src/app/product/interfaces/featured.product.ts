/* Featured product is used for displaying at the dashboard products
 * of different types.*/
export interface FeaturedProduct {
	id: string,
	prize: number,
	url: string,
	description: string,
	pType: string
}